<p id="notice"><%= notice %></p>

<h1>Generic Tolls</h1>
<h4>This page is dedicated to the generic tolls description - you can see the existent list of generic tolls, modify them or introduce new ones.</h4>
<ul>
 
  <li>
  <p>
  <h4> Download All Generic Tolls as CSV or Excel file:
  <%= link_to "CSV", generic_tolls_path(format: "csv") %> |
  <%= link_to "Excel", generic_tolls_path(format: "xls") %> 
  </h4> 
  </p>
  </li>


  <li>
  <div>
  <h4> Import Generic Tolls from CSV file: </h4>
    <%= form_tag import_generic_tolls_path, multipart: true do %>
    <%= file_field_tag :file %>
    <%= submit_tag  "Import CSV"%>
  <% end %>
  </div>
  </li>

  <li>
  <p>
  <h4> Create <%= link_to 'New Generic Toll ',new_generic_toll_path %> </h4> 
  </p>
  </li>

</ul>

<table>
  <thead>
    <tr>
      <th>Startdate</th>
      <th>Enddate</th>
      <th>Km</th>
      <th>Eur</th>
      <th>Truck Nb Plate</th>
      <th>Country</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @generic_tolls.each do |generic_toll| %>
      <tr>
        <td><%= generic_toll.StartDate %></td>
        <td><%= generic_toll.EndDate %></td>
        <td><%= generic_toll.Km %></td>
        <td><%= generic_toll.EUR %></td>
        <td><%= @trucks.find(generic_toll.truck_id).NB_PLATE %></td>
        <td><%= generic_toll.country %></td>
        <td><%= link_to 'Show', generic_toll %></td>
        <td><%= link_to 'Edit', edit_generic_toll_path(generic_toll) %></td>
        <td><%= link_to 'Destroy', generic_toll, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>