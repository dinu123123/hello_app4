<p id="notice"><%= notice %></p>

<h1>Trailers</h1>

<table>
  <thead>
    <tr>
      <th>Nb plate</th>
      <th>Info</th>
      <th>Chassis</th>
      <th>Fabdate</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trailers.each do |trailer| %>
      <tr>
        <td><%= trailer.NB_PLATE %></td>
        <td><%= trailer.INFO %></td>
        <td><%= trailer.CHASSIS %></td>
        <td><%= trailer.FABDATE %></td>
        <td><%= link_to 'Show', trailer %></td>
        <td><%= link_to 'Edit', edit_trailer_path(trailer) %></td>
        <td><%= link_to 'Destroy', trailer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Trailer', new_trailer_path %>
