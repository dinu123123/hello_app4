<!DOCTYPE html>
<html>
<head>
<style>
table {
    font-family: arial, sans-serif;
    border-collapse: collapse;
    width: 80%;
}

td, th {
    border: 1px solid #aaaaaa;
    text-align: left;
    padding: 1px;
}

tr:nth-child(even) {
    background-color: #dddddd;
}
</style>
</head>
<body>

<p id="notice"><%= notice %></p>
<h1>Trucks</h1><h4>This page is dedicated to the trucks description - you can see the existent list of trucks, modify them or introduce new ones.</h4>
<h3>
<%= link_to 'New Truck ',new_truck_path %> 
</h3>
</br>

<table>
  <thead>
    <tr>
      <th>Nb Plate</th>
      <th>Chassis</th>
      <th>Fabrication</th>
      <th>Info</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trucks.each do |truck| %>
      <tr>
        <td><%= truck.NB_PLATE %></td>
        <td><%= truck.CHASSIS %></td>
        <td><%= truck.FABDATE %></td>
        <td><%= truck.INFO %></td> 
        <td><%= link_to 'Show', truck %></td>
        <td><%= link_to 'Edit', edit_truck_path(truck) %></td>
        <td><%= link_to 'Destroy', truck, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

