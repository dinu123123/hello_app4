<!DOCTYPE html>
<html>
<head>
<style>
table {
    font-family: arial, sans-serif;
    border-collapse: collapse;
    width: 80%;
}

td, th {
    border: 1px solid #aaaaaa;
    text-align: left;
    padding: 1px;
}

tr:nth-child(even) {
    background-color: #dddddd;
}
</style>
</head>
<body>

<p id="notice"><%= notice %></p>
<h1>Trucks</h1>
<h4>This page is dedicated to the trucks description - you can see the existent list of trucks, modify them or introduce new ones.</h4>

<ul>
 
  <li>
  <p>
  <h4> Download All Trucks as CSV or Excel file:
  <%= link_to "CSV", trucks_path(format: "csv")  %> |
  <%= link_to "Excel", trucks_path(format: "xls") %> 
  </h4> 
  </p>
  </li>


  <li>
  <div>
  <h4> Import Trucks from CSV file: </h4>
    <%= form_tag import_trucks_path, multipart: true do %>
    <%= file_field_tag :file %>
    <%= submit_tag  "Import CSV"%>
  <% end %>
  </div>
  </li>

  <li>
  <p>
  <h4> Create <%= link_to 'New Truck ',new_truck_path %> </h4> 
  </p>
  </li>

</ul>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Nb Plate</th>
      <th>Chassis</th>
      <th>Fabrication</th>
      <th>Info</th>
       <th>Photos</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trucks.each_with_index do |truck, i| %>
      <tr>
        <td><%= i+1 %></td>
        <td><%= truck.NB_PLATE %></td>
        <td><%= truck.CHASSIS %></td>
        <td><%= truck.FABDATE %></td>
        <td><%= truck.INFO %></td> 
        <td><%= truck.images.count %></td>
        <td><%= link_to 'Show', truck %></td>
        <td><%= link_to 'Edit', edit_truck_path(truck) %></td>
        <td><%= link_to 'Destroy', truck, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
